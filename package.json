{
  "devDependencies": {
    "@babel/preset-env": "^7.29.7",
    "css-loader": "^7.1.4",
    "gh-pages": "^6.3.0",
    "html-loader": "^5.1.0",
    "html-webpack-plugin": "^5.6.7",
    "jest": "^30.4.2",
    "style-loader": "^4.0.0",
    "webpack": "^5.107.2",
    "webpack-cli": "^7.0.3",
    "webpack-dev-server": "^5.2.5"
  },
  "scripts": {
    "dev": "webpack serve",
    "deploy": "webpack && git checkout gh-pages && rm -rf * && cp -r dist/* . && git add . && git commit -m \"deploy\" || echo \"no changes\" && git push origin gh-pages && git checkout main"
  }
}